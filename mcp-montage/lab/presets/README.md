# presets

- `styles/` — палитра, типографика, motion language (из референса)
- `subtitles/` — параметры Text+/Fusion titles
- `resolve/` — экспортируемые пресеты Resolve (когда появятся файлы)

Пока референс не разобран покадрово — не заполнять выдуманными значениями.
